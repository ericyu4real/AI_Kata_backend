first step: install dependencies - pip install -r requirements.txt
second step: run main.py
third: download postman, add a post method and link to your ip: http://127.0.0.1:5000/query
last but not least: add a variable named "query" in the body, asking anything you want and press "send"